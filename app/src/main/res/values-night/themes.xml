<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base dark theme for the app -->
    <style name="Base.Theme.ProjectManager" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Màu tối, chữ sáng -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/black</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="colorSurface">@color/black</item>
        <item name="colorOnSurface">@color/white</item>

        <!-- Nền toàn app (full-screen) -->
        <item name="android:windowBackground">@color/black</item>

        <!-- Viền, ripple, tertiary -->
        <item name="colorOutline">@color/white</item>
        <item name="colorTertiary">@color/black</item>
        <item name="colorOnTertiary">@color/white</item>

        <!-- Nền cho item được chọn (ví dụ bottom nav) -->
        <item name="colorSurfaceVariant">@color/black</item>

        <item name="popupMenuStyle">@style/PlainPopupMenu</item>
        <item name="android:popupMenuStyle">@style/PlainPopupMenu</item>

    </style>

</resources>
